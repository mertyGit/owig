[main]
sleep=1000             ; miliseconds to sleep between screen graps

[output]
stats="owig_stats.csv" ; file to append statistics to
divider=","            ; divider to use between fields
header=true            ; Generate headerfields on first line

[debug]
screen=false           ; Screen intepretation info
ocr=false              ; Character reading
window=true            ; Windows call debug
pause=1000             ; Pause between reading screenshot files
